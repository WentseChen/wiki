# natural policy gradient

A second-order RL optimization method that preconditions the policy gradient by the inverse Fisher Information Matrix, producing updates that are covariant with respect to the policy's parameterization and typically converge faster than vanilla policy gradients.

## Sources
- [[rank1-fisher-natural-policy-gradient]]
