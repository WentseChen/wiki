# shrink-and-perturb

A one-shot reset transformation `w := λw + ε·noise` (with λ < 1) applied to a converged network before continued training. Closes the warm-start generalization gap and is the supervised-side ancestor of continual-backprop-style methods.

## Sources
- [[warm-starting-shrink-perturb]]
