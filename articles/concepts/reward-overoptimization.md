# Reward Overoptimization

The phenomenon where a policy trained against a learned reward model exploits the model's errors, achieving high reward proxy scores while degrading actual alignment quality. Also called reward hacking. KL regularization and explicit uncertainty penalties (e.g., via LoRA ensembles) are common mitigations.

## Sources
- [[up-rlhf-diverse-lora-ensembles]]
