# neural collapse

A phenomenon in trained deep classifiers where within-class feature embeddings converge to a single point (DNC1), the class means form a simplex ETF structure, and the classifier weights align with the class means — empirically observed and now provably linked to L² regularization.

## Sources
- [[neural-collapse-low-rank-bias]]
