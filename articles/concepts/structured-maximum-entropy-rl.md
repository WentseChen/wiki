# Structured maximum-entropy RL

Variant of max-ent RL where entropy is induced over a latent variable conditioning the policy rather than (only) over actions — yields multiple distinct near-optimal solutions to a single MDP, each indexed by a latent value.

## Sources
- [[smerl-structured-maxent-rl]]
